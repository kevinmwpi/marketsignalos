# Set service root to /apps/web and config file to /apps/web/railway.toml.
# Use the HTTPS domain Railway assigns to the API service, including scheme.
API_BASE_URL=https://YOUR-API-SERVICE.up.railway.app
NODE_ENV=production
# No ADMIN_API_TOKEN on this service. Public requests never acquire credentials.
